import 'package:flutter/material.dart';

class CompanyTour201DataV4 extends StatelessWidget {
  const CompanyTour201DataV4({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text(
          'By following these steps, you can set up a tour with a company you are interested in working for and gain valuable insights into the workplace and company culture.',
          style: TextStyle(
            color: Colors.white,
            fontSize: 20,
            fontFamily: 'Montserrat',
          ),
        ),
      ],
    );
  }
}
