import 'package:flutter/material.dart';

class DiversityInclusion401DataV4 extends StatelessWidget {
  const DiversityInclusion401DataV4({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Padding(
          padding: const EdgeInsets.only(top: 20.0),
          child: Text(
            'Equity acknowledges that different individuals or groups may require different levels of support or accommodations to achieve equality. It involves identifying and addressing systemic barriers and biases that may prevent certain individuals or groups from fully participating and thriving.',
            style: TextStyle(
              color: Colors.white,
              fontSize: 20,
              fontFamily: 'Montserrat',
            ),
          ),
        ),
      ],
    );
  }
}
